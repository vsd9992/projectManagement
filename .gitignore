# Secrets & local config — never commit credentials, keys, or kubeconfigs
.env
.env.*
!.env.example
*.pem
*.key
kubeconfig*
secrets/
hyperv-user
hyperv-password

# Rust
/target/
**/target/

# Node / frontend
node_modules/
dist/
build/
